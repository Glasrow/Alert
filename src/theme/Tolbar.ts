import styled from "styled-components"


const Tolbar = styled.article`
	width: 440px;
	height: 790px;
    background: #FFFFFF; /*не работает сука*/
	box-shadow: 0px 5px 30px rgba(0, 0, 0, 0.1);
	border-radius: 40px;
`

export default Tolbar